[project]
name = "taxi"
version = "0.1.0"
description = "Local environment for the taxi analysis notebook"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.5",
    "python-dotenv>=1.1.1",
]

[tool.uv]
package = false
